h1 User list

form(action='/users/search', method='GET')
  input(type='text', name='q', value=contentSearch)
  button Search

ul
  each user in users
    li
      span= user.name
      span &nbsp
      a(href='/users/' + user.id) View 

a(href='/users/create') New